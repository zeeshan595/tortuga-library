image: gcc

build:
  stage: build
  script:
    - apt-get update
    - apt-get -y install cmake protobuf-compiler
    - apt-get -y install libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev libwayland-client0 libwayland-dev
    - make init
    - make