{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "debug",
            "type": "shell",
            "command": "cd ../Tortuga/ && make && cd ${workspaceFolder} && make",
            "group": "build",
            "problemMatcher": "$gcc"
        },
        {
            "label": "build",
            "type": "shell",
            "command": "cd ../Tortuga/ && make build && cd ${workspaceFolder} && make build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$gcc"
        }
    ]
}